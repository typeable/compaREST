# **POST** /test

## JSON Request

### `$[?(typeOf @ == "Object")]`

Expected the property `property2` to be allowed, but it wasn't.

## JSON Response – 200

### `$[?(typeOf @ == "Object")]`

Don't have a required property `property2`.

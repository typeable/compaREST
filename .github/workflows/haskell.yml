name: Haskell CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  linux-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Cache
        uses: actions/cache@v1
        with:
          path: ~/.stack
          key: ${{ runner.os }}-build-stack-${{ hashFiles('**/stack.yaml') }}-${{ hashFiles('**/*.cabal') }}
          restore-keys: |
            ${{ runner.os }}-build-stack-${{ hashFiles('**/stack.yaml') }}
            ${{ runner.os }}-build-stack-
      - name: Build
        run: |
          rm -rf ~/.stack
          stack build --ghc-options="-O3 -j -split-sections" --local-bin-path openapi-diff-linux --copy-bins --flag openapi-diff:Static
      - name: Test
        run: stack test --test-arguments='--hide-successes --no-create'

      - name: Zip
        if: github.ref == 'refs/heads/master'
        run: |
          zip openapi-diff-linux openapi-diff-linux/openapi-diff
      - uses: "marvinpinto/action-automatic-releases@latest"
        if: github.ref == 'refs/heads/master'
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: false
          title: "Pre-Release"
          files: |
            *.zip
